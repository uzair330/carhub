import { CustomFiltersProps } from '@/types/types'
import React from 'react'

const CustomFilter = ({title,options}:CustomFiltersProps) => {
  return (
    <div>
      Custom filter
    </div>
  )
}

export default CustomFilter
